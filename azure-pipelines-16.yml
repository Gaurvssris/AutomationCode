
# trigger:
# - main

# pool:
#   name: AgentMay

# steps:

# - task: TerraformTask@5
#   inputs:
#     provider: 'azurerm'
#     command: 'init'
#     workingDirectory: '$(System.DefaultWorkingDirectory)/Root/Dev'
#     backendServiceArm: 'automation'
#     backendAzureRmStorageAccountName: 'gauravstrg'
#     backendAzureRmContainerName: 'gauravcont'
#     backendAzureRmKey: 'backup.tfstate'
trigger:
- main

pool:
  name: AgentMay

stages:

# =========================
# DEV STAGE
# =========================
- stage: DEV
  displayName: "Terraform Init - DEV"

  jobs:
  - job: DevInit
    displayName: "Terraform Init Dev"

    steps:

    - task: TerraformTask@5
      displayName: "Terraform Init DEV"
      inputs:
        provider: 'azurerm'
        command: 'init'
        workingDirectory: '$(System.DefaultWorkingDirectory)/Root/Dev'

        backendServiceArm: 'automation'

        backendAzureRmStorageAccountName: 'gauravstrg'
        backendAzureRmContainerName: 'gauravcont'
        backendAzureRmKey: 'dev.tfstate'

# =========================
# QA STAGE
# =========================
- stage: QA
  displayName: "Terraform Init - QA"

  dependsOn: DEV

  jobs:
  - job: QaInit
    displayName: "Terraform Init QA"

    steps:

    - task: TerraformTask@5
      displayName: "Terraform Init QA"
      inputs:
        provider: 'azurerm'
        command: 'init'
        workingDirectory: '$(System.DefaultWorkingDirectory)/Root/QA'

        backendServiceArm: 'automation'

        backendAzureRmStorageAccountName: 'gauravstrg'
        backendAzureRmContainerName: 'gauravcont'
        backendAzureRmKey: 'qa.tfstate'

# =========================
# PROD STAGE
# =========================
- stage: PROD
  displayName: "Terraform Init - PROD"

  dependsOn: QA

  jobs:
  - job: ProdInit
    displayName: "Terraform Init PROD"

    steps:

    - task: TerraformTask@5
      displayName: "Terraform Init PROD"
      inputs:
        provider: 'azurerm'
        command: 'init'
        workingDirectory: '$(System.DefaultWorkingDirectory)/Root/Prod'

        backendServiceArm: 'automation'

        backendAzureRmStorageAccountName: 'gauravstrg'
        backendAzureRmContainerName: 'gauravcont'
        backendAzureRmKey: 'prod.tfstate'